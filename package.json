{
  "name": "my-personal-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-blog.js",
    "deploy": "node scripts/build-blog.js && git add -A && git diff --cached --quiet || git commit -m \"content: update articles\" && git push"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
