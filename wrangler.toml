name = "myprofoilo"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# 纯静态资源部署，不打包任何 Worker 脚本
[assets]
directory = "."
not_found_handling = "404-page"

# 排除不需要上传的目录
[assets.ignore]
patterns = ["node_modules/**", "scripts/**", "content/**", "tech_blog/**", ".git/**"]
